import 'package:flutter/material.dart';

class ModifierTransactionWidget extends StatelessWidget {
  const ModifierTransactionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(child: Column());
  }
}
